# MCP Servers

List of MCP servers available for use with Claude.

## Angular CLI MCP Server

Run below command to add Angular CLI MCP server to your Claude setup:

```bash
claude mcp add angular-cli -- npx @angular/cli mcp
```
